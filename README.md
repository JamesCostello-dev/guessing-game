Guessing Game is a simple command line application built with Rust.

- Install Instructions
    - clone repo
    - cargo run, to run application
    - cargo build to build application
    - follow prompts
    - type quit to exit
